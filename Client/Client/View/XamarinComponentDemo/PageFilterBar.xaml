<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Client.View.XamarinComponentDemo.PageFilterBar"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:Client.Components"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="IsTextChangeExecute == false" />
            <components:FilterBar SearchCommand="{Binding SearchCommand}" />


            <Label Text="IsTextChangeExecute == true" />
            <components:FilterBar
                IsTextChangeExecute="True"
                SearchCommand="{Binding SearchCommand}" />

            <ListView
                x:Name="searchResults"
                ItemsSource="{Binding SearchResults}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>