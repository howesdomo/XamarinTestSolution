<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Client.View.XamarinComponentDemo.PageXamarinComponentMenu"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="控件使用实例"
    mc:Ignorable="d">
    <ContentPage.Content>
        <StackLayout>

            <Button
                Command="{Binding BtnPageLabel_Command}"
                Text="Label" />

            <Button
                Command="{Binding BtnPageEntry_Command}"
                Text="Entry" />

            <Button
                Command="{Binding BtnPageStepper_Command}"
                Text="Stepper" />

            <Button
                Command="{Binding BtnPageSearchBar_Command}"
                Text="Search Bar" />

            <Button
                Command="{Binding BtnPageFilter_Command}"
                Text="Filter Bar(自制控件)" />

            <Button
                Command="{Binding BtnWebViewAdv_Command}"
                Text="WebViewAdv(优化版WebView[目前只有安卓])" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>